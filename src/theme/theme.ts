import { createTheme } from "@mui/material"

export const theme = createTheme({
  palette: {
    primary: {
      main: "#007FFF",
      dark: "#00448B",
    },
    secondary: {
      main: "#000000",
    },
    error: {
      main: "#f44336",
    },
  },
})
